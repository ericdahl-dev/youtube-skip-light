// Copy to secrets.h and fill in. secrets.h is gitignored.
#pragma once

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"

// Guards over-the-air firmware updates. Without it, anyone on your LAN could
// flash arbitrary firmware to the board.
#define OTA_PASSWORD "CHANGE_ME"
